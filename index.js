var React = require('react');

var Presentation = React.createClass({
  displayName: 'Presentation',
  render() {
    return (<div></div>);
  }
});

React.render(<Presentation />, document.getElementById('main'))
